type: app
name: frontend

build:
  context: ../../remote-team-web
  dockerfile: Dockerfile

port: 3000

env:
  NEXT_PUBLIC_API_URL: https://test-braingig-crm-backend.vvowhz.easypanel.host/graphql
  NEXT_PUBLIC_WS_URL: wss://test-braingig-crm-backend.vvowhz.easypanel.host

